<?xml version="1.0" encoding="utf-8"?>
<!--
  Android bloquea HTTP sin cifrar desde API 28. El backend local para
  desarrollo (10.0.2.2 desde el emulador, o localhost con adb reverse) no
  tiene HTTPS, así que se habilita cleartext solo para esas direcciones,
  no para cualquier servidor.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
