package com.mitienda.ecommerce.models;

/**
 * Enum para el origen del pedido
 */
public enum OrigenPedido {
    WEB,      // Pedido desde la tienda virtual
    MANUAL    // Pedido creado por admin (telefónico/presencial)
}