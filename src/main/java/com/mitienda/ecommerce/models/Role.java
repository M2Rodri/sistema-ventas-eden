package com.mitienda.ecommerce.models;

/**
 * Enum que define los roles de usuario en el sistema
 */
public enum Role {
    ADMIN,      // Administrador del sistema
    EMPLEADO,      // Administrador del sistema LIMITADO 
    CLIENTE     // Cliente que compra productos
}